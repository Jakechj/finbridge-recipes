# Claude.ai / Claude Desktop

Settings → Connectors → **Add custom connector** → name `FinBridge`, URL `https://mcp.gronox.kr/mcp` → sign in with Google. The free plan starts at once. Then open a new chat, **turn FinBridge on in that chat (+ → FinBridge — it is off in every new conversation until you do)**, and paste a recipe prompt.
